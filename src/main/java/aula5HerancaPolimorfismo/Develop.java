package aula5HerancaPolimorfismo;

public record Develop(double extra){
    //Record não trabalha com herança de classe
    //não tem propriedades dentro, somente ser for static (e métodos static não são passados por herança)
}
