package desafiosLivresIA;

import java.util.Arrays;

public class Kata1 {

    public static Boolean isSatorSquare(char[][] tablet) {

        // char[][] é um array bidimensional de caracteres — basicamente uma matriz onde cada posição guarda um char
        Boolean isSatorSquare = false;

        for(int linha=0; linha < tablet.length; linha++){
            String palavra = Arrays.toString(tablet[linha]);
            for(int coluna=0; coluna < tablet[linha].length; coluna++ ){


            }
        }

        return isSatorSquare;
    }

}

